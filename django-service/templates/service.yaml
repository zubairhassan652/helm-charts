apiVersion: v1
kind: Service
metadata:
  name: {{ include "django-service.fullname" . }}
  labels:
    {{- include "django-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "django-service.selectorLabels" . | nindent 4 }}
